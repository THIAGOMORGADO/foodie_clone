import { theme } from "../../constants/theme"

export const styles = {
  headerContainer: {
    alignItems: "center",
    width: "100%",
    
  },
  logo: {
    width: 170,
    height: 50,
  },
  title: {
    colors: theme.colors.darkGray,
    fontSize: theme.FONT_SIZE.md,
    textAlign: "center",
  },
}
