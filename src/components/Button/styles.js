import { theme } from "../../constants/theme";

export const styles = {
  btn: {
    width: "100%",
    backgroundColor: theme.colors.primary,
    borderRadius: 6
  },
  buttonText: {
    textAlign: "center",
    fontSize: theme.FONT_SIZE.md,
    color: '#ffffff',
    textAlign: 'center',
    padding: 14,
  }
}